# ===================================================================
# WEBDAV SERVER CONFIGURATION TEMPLATE FOR LUKS MANAGER
# Placeholders (${WEBDAV_USER}, ${WEBDAV_PASSWORD_HASH}, ${MOUNT_CRYPTO}, ${WEBDAV_PORT})
# are dynamically populated by scripts/install-webdav.sh from .env
# ===================================================================
address: 0.0.0.0
port: ${WEBDAV_PORT}
cert: ""
key: ""
auth: true

users:
  - username: "${WEBDAV_USER}"
    password: "${WEBDAV_PASSWORD_HASH}"
    scope: "${MOUNT_CRYPTO}"
    modify: true
